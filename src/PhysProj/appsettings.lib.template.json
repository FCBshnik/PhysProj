{
  // Create beside file appsettings.lib.dev.json and fill it based on this template
  // appsettings.lib.dev.json is used by all startable Library apps
  // appsettings.lib.dev.json passed to configuration via command line args in launchSettings.json file of app
  // appsettings.lib.dev.json added to gitignore to not expose dev settings in repo
  "ConnectionStrings": {
    "mongo-dev": "mongodb://localhost:27017",
    "mongo-dev-bak": "mongodb://localhost:27017/phys-lib-bak",
    "mongo": "mongo-dev,mongo-dev-bak",
    "postgres": "Host=localhost;Username=;Password=;Database=phys-lib",
    "db": "mongo-dev",
    "rabbitmq": "localhost",
    "meilisearch": "http://localhost:7700/",
    "works-files": "<path-to-books-files>",
    "logs_elastic": "http://localhost:9200/"
  }
}
